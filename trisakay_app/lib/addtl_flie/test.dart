import 'package:flutter/material.dart';

class trylangito extends StatelessWidget {
  const trylangito({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
    );
  }
}